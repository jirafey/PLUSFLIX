<form method="post" action="{{ path('app_streaming_delete', {'id': streaming.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ streaming.id) }}">
    <button class="btn">Delete</button>
</form>
